name: flutter_opendroneid_example
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.16.7"

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^12.0.0
  flutter_opendroneid:
    path: ../

flutter:
  uses-material-design: true
